presubmits:
rickydjohn/profile:
- name: deploy-prow
  decorate: true
  always_run: true
  skip_report: false
  branches:
    - '^main$'
  spec:
    containers:
    - image: alpine
      command: ["sleep", "200"]
